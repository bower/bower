var Emitter = require('events').EventEmitter;

function help(name) {
    var emitter = new Emitter();

    // TODO
    return emitter;
}

// -------------------

module.exports = help;

module.exports.line = function (argv) {
    // TODO
    return help();
};