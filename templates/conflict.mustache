
{{#yellow}}Unable to find a suitable version for {{name}}:{{/yellow}}
{{#packages}}
    {{#version}}
    {{#cyan}}{{manager.name}}{{/cyan}} requires {{name}} {{#white}}v{{version}}{{/white}}
    {{/version}}
{{/packages}}

In order to resolve this, you need to do one of two things:
    * Manually add either {{name}} {{versions}} to your {{json}} file
    * Run this command again with the --save and --force-latest flags