{
    "command": "uninstall",
    "description": "Uninstalls a package locally from your bower_components directory",
    "usage": [
        "uninstall <name> [<name> ..] [<options>]"
    ],
    "options": [
        {
            "shorthand":   "-h",
            "flag":        "--help",
            "description": "Show this help message"
        },
        {
            "shorthand":   "-s",
            "flag":        "--save",
            "description": "Save installed packages into the project's package.json bowerDependencies"
        },
        {
            "shorthand":   "-D",
            "flag":        "--save-dev",
            "description": "Save installed packages into the project's package.json devbowerDependencies"
        }
    ]
}
